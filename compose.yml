services:
  web:
    image: caddy:2-alpine
    container_name: zampolit73-web
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./app:/srv:ro
